#include <string.h>
#include "main.h"

/**
  * _strcmp - compares 2 stings
  * @s1: string 1
  * @s2: string 2
  *
  * Return: +ve if the s1 > s2 or -ve if s1 < s2 or 0 otherwise
  */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
