#include <string.h>
#include "main.h"

/**
  * _strcat - concatinate 2 strings
  * @dest: the destination string
  * @src: the source string
  *
  * Return: pointer to that concatinated string
  */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
