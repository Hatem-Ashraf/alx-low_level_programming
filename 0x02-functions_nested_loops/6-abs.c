#include "main.h"
/**
  * _abs - return the absolute of a number
  * @n: number to be processed
  *
  * Return: the number after processing
  */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
	}
	return (n);
}
