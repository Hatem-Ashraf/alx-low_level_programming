#include <stdio.h>
#include <string.h>
#include "main.h"

/**
  * _strcpy - copy
  * @dest: dest str
  * @src: src str
  *
  * Return: pointer to char
  */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));

}
